# Image-using-ASCII-code
This project demonstrates pattern matching and conditional programming by recreating a 41-line ASCII art image (120 characters per line) using nested loops and position-based conditionals. Each character position is determined through if-elif-else chains based on row and column indices.
